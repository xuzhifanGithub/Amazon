# 历史结果说明

本目录已有的 10 局结果生成于 2026-08-26。当时测试配置通过新版引擎直接读取了误放在
`amazon_L` 路径中的 `amazon18-s2161408-d449231` 权重，因此棋谱里的
`amazon_L_b18` **不代表原始 amazon_L**。身份核验表明，amazon18 是
2026-07-26 已废弃的 b18c256 早期自训练模型。

这些棋谱仅作为历史诊断记录保留，不应继续用于评价 amazon_L。原始 L 是
`amazons8x-b20c256-s182755840-d39606896`；服务器 `initial_fixed` 也是同一组张量的格式兼容版。
历史记录名 `amazon_L_original_z` 中的 `_z` 只是当时的 GUI 别名，不代表另一套模型。
