# 更新日志

本项目采用中文提交说明；完整变更历史可在 Git 日志中查看。

## Unreleased

- 前端保留 `MCTS★`、`MCTS-18特征★★`、`amazon_L★★★` 与 `amazon_X★★★★`，
  只移除重复的 Z；提示模型使用 L/X。
- 修复 L/X 引擎初始化完成后读取未定义后端信息、导致两种模型均无法开始搜索的问题。
- 修正 L 的权重映射，使其加载 `amazons8x-b20c256-s182755840-d39606896`，并移除误作 L
  的 `amazon18-s2161408-d449231` 归档权重。
- 移除 AI 思考时间与 visits 的人工上限和固定档位取整，仅要求输入为正数。
- 修复基础 MCTS 将未归一化旧估值直接用于 UCT、在劣势局面可能空指针闪退的问题；基础估值现稳定映射到 `[-1, 1]`。
- 核清模型身份：原始 L 和服务器 `initial_fixed` 都是 `amazons8x-b20c256-s182755840-d39606896`
  的同一组张量；Z 只是服务器参考别名，不是独立于 L 的第三套强网。
- 确认 `amazon18-s2161408-d449231` 是 2026-07-26 已废弃的 b18c256 早期自训练归档，
  不是原始 L。
- 精简 L/X 前端摘要，移除重复的效用值，并提示首次 OpenCL 调优可能耗时数分钟。
- L 的旧训练标签不提供可信分差；前端仅对 X 展示预测分差。
- 在 X 分析面板按 `scoreSelfplay` 正确显示预计分差、分差不确定度、效用和策略先验。
- 修复原始模型在低 visits 局面误返回 `pass` 导致对局失败的问题。
- 强制 AI 仅提交亚马逊棋坐标，不允许 `pass` 或 `resign` 结束对局。
- 更新 `amazon_X` 模型，完善 AI 对 AI、新游戏、悔棋和后台请求生命周期。
- 新增独立 AI 参数、Top-N 三阶段胜率提示、可缩放棋盘和卡片式信息面板。
- 新增棋谱导入导出、诊断日志、资源预检和 Windows 便携构建脚本。
