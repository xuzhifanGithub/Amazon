# 更新日志

本项目采用中文提交说明；完整变更历史可在 Git 日志中查看。

## Unreleased

- 新增 `amazon_Z★★★★`，使用服务器评测中的 `amazon18-s2161408-d449231` 强模型，并接入黑白对弈、提示和便携发布流程。
- 将随包的 `amazon_L` 权重从误配的 `amazon18` 恢复为最初的 L 模型 `amazons8x-b20c256-s182755840-d39606896`，并改用兼容引擎读取外部权重，使直接替换 L 模型真实生效。
- 精简 amazon_X/Z/L 前端摘要，移除重复的效用值，并提示首次 OpenCL 调优可能耗时数分钟。
- amazon_L/Z 虽保留结构上的分差头，但旧训练标签无效；前端仅对 amazon_X 展示预测分差。
- 在 amazon_X 分析面板按 `scoreSelfplay` 正确显示预计分差、分差不确定度、效用和策略先验。
- 新增独立的 18 特征 MCTS 模型，前端 AI 按棋力显示为 1—4 星。
- 统一前端模型名称为 `amazon_X`、`amazon_Z` 与 `amazon_L`。
- 修复原始模型在低 visits 局面误返回 `pass` 导致对局失败的问题。
- 强制 AI 仅提交亚马逊棋坐标，不允许 `pass` 或 `resign` 结束对局。
- 更新 `amazon_X` 模型，完善 AI 对 AI、新游戏、悔棋和后台请求生命周期。
- 新增独立 AI 参数、Top-N 三阶段胜率提示、可缩放棋盘和卡片式信息面板。
- 新增棋谱导入导出、诊断日志、资源预检和 Windows 便携构建脚本。
