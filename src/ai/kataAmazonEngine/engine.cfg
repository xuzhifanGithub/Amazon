# KataAmazon GTP config - single GPU, 10x10
numSearchThreads = 8
maxVisits = 400

numNNServerThreadsPerModel = 1
gpuToUseThread0 = 0
nnCacheSizePowerOfTwo = 20
nnMutexPoolSizePowerOfTwo = 18

# 必须禁用认输：本项目把一个亚马逊棋回合编码为 3 次交替颜色的 genmove
# （b 选子 / w 移动 / b 放箭），中间那次 genmove 会让引擎以对手视角评估局面，
# 一旦该视角看起来必败，引擎就会返回 resign 而不是坐标，导致回合无法解析。
allowResignation = false
# 下面两个键即使禁用认输也必须存在，否则引擎启动时报
# "Could not find key 'resignThreshold'" 并退出。
resignThreshold = -0.90
resignConsecTurns = 3
lagBuffer = 1.0
ponderingEnabled = false

# --- Logging ---
logDir = gtp_logs
logAllGTPCommunication = false
logSearchInfo = false
logToStderr = false

# Report winrate during analysis in centipct-ish; default reportAnalysisWinratesAs = SIDETOMOVE
reportAnalysisWinratesAs = SIDETOMOVE
